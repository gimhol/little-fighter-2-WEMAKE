import type { TNextFrame } from './INextFrame';
export interface IHoldKeyCollection {
  a?: TNextFrame;
  d?: TNextFrame;
  j?: TNextFrame;
  U?: TNextFrame;
  D?: TNextFrame;
  L?: TNextFrame;
  R?: TNextFrame;
  B?: TNextFrame;
  F?: TNextFrame;
}
